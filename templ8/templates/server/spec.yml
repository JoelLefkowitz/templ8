root_name: server

required_config:
  - backend_name

required_config_extends:
  - common

path_replacements:
  - backend: <backend_name>

callbacks:
  django_callback: django-admin startproject <backend_name> <backend_name>
