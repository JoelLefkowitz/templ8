name: frontend

required_context:
  frontend_name:

extends:
  - common

path_replacements:
  frontend: <frontend_name>

callbacks:
  yarn_callback: ng config -g cli.packageManager yarn
  angular_callback: ng new --routing=true --style=scss <frontend_name> --directory <frontend>
