name: backend

required_context:
  backend_name:

required_config_extends:
  - common

path_replacements:
  - backend: <backend_name>
  - backend: <backend_name>
  - backend: <backend_name>
  - backend: <backend_name>

callbacks:
  - django: django-admin startproject  <backend_name> <backend_name>

